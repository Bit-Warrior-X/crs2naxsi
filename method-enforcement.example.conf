# CRS REQUEST-911-METHOD-ENFORCEMENT (id 911100) has no NAXSI MainRule form:
# ModSec uses REQUEST_METHOD !@within %{tx.allowed_methods}.
# Enforce allowed methods in nginx instead, e.g.:

if ($request_method !~ ^(GET|HEAD|POST|OPTIONS)$) {
    return 405;
}
