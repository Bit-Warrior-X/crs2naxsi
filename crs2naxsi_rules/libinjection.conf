# Native NAXSI replacements for CRS @detectSQLi / @detectXSS
# Place inside the protected location{}:
LibInjectionSql;
LibInjectionXss;

# CRS 941100: enable `LibInjectionXss;` in the location (native NAXSI equivalent of @detectXSS) — XSS Attack Detected via libinjection
# CRS 942100: enable `LibInjectionSql;` in the location (native NAXSI equivalent of @detectSQLi) — SQL Injection Attack Detected via libinjection
